name: Deprecated Deployment Template

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Fail with migration note
        run: |
          echo "This workflow is deprecated. Use deploy-worker.yml instead."
          exit 1
